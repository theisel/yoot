## API Report File for "@yoot/yoot"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import {Primitive as Primitive_2} from './types';

// @public
export type Adapter = {
  supports: (url: URL) => boolean;
  generateUrl: (input: GenerateUrlInput) => string;
  primeState?: (input: PrimeStateInput) => YootState;
};

// @public
export type AdapterOptions = {
  [Key in keyof Adapter]: Adapter[Key];
};

// @public
function buildSrcSet(options: BuildSrcSetOptions, yoot: Yoot): string;

// @public
interface BuildSrcSetOptions {
  densities?: number[];
  widths?: number[];
}

// @public
export function createAdapter(options: AdapterOptions): Adapter;

// @public
export const defineConfig: (options: YootConfig) => void;

// @public
function defineSrcSetBuilder(options: BuildSrcSetOptions): (yoot: Yoot) => string;

// @public
export type DirectiveHandler<K extends keyof Directives> = (value: NonNullable<Directives[K]> | null) => Yoot;

// @public
export interface DirectiveMethods {
  ar: DirectiveHandler<'aspectRatio'>;
  aspectRatio: DirectiveHandler<'aspectRatio'>;
  crop: DirectiveHandler<'crop'>;
  dpr: DirectiveHandler<'dpr'>;
  fit: DirectiveHandler<'fit'>;
  fm: DirectiveHandler<'format'>;
  format: DirectiveHandler<'format'>;
  h: DirectiveHandler<'height'>;
  height: DirectiveHandler<'height'>;
  q: DirectiveHandler<'quality'>;
  quality: DirectiveHandler<'quality'>;
  w: DirectiveHandler<'width'>;
  width: DirectiveHandler<'width'>;
}

// @public
export type DirectiveNames = keyof Directives;

// @public
export type Directives = {
  aspectRatio?: number;
  crop?: 'center' | Vertical | Horizontal;
  dpr?: number;
  fit?: 'contain' | 'cover';
  format?: 'auto' | 'jpg' | 'png' | 'webp';
  height?: number;
  quality?: number;
  width?: number;
};

// @public
export type GenerateUrlInput = {
  src: string;
  directives: Directives;
};

// @internal
export function _getAdapter(url: URL): Adapter | never;

// Warning: (ae-forgotten-export) The symbol "Attrs" needs to be exported by the entry point api-extractor.d.ts
//
// @internal
function getAttrs(yoot: Yoot): Attrs;

// @public
function getImgAttrs(yoot: Yoot, options?: ImgAttrsOptions): ImgAttrs;

// @public
function getImgAttrs_2(yoot: Yoot, options?: ImgAttrsOptions): ImgAttrs_2;

// Warning: (ae-forgotten-export) The symbol "MimeType_2" needs to be exported by the entry point api-extractor.d.ts
//
// @internal
function getMimeType(yoot: Yoot): MimeType_2 | undefined;

// @public
function getSourceAttrs(yoot: Yoot, options?: SourceAttrsOptions): SourceAttrs;

// @public
function getSourceAttrs_2(yoot: Yoot, options?: SourceAttrsOptions): SourceAttrs_2;

// Warning: (ae-internal-missing-underscore) The name "hasIntrinsicDimensions" should be prefixed with an underscore because the declaration is marked as @internal
//
// @internal
export function hasIntrinsicDimensions<
  Dimensions extends {
    width: number;
    height: number;
  },
  Input extends Partial<Dimensions>,
>(input?: Input): input is Dimensions & Input;

// @public
export type Horizontal = 'left' | 'right';

declare namespace html {
  export {
    buildSrcSet,
    defineSrcSetBuilder,
    getImgAttrs_2 as getImgAttrs,
    getSourceAttrs_2 as getSourceAttrs,
    withImgAttrs_2 as withImgAttrs,
    withSourceAttrs_2 as withSourceAttrs,
    BuildSrcSetOptions,
    ImgAttrs_2 as ImgAttrs,
    ImgAttrsOptions,
    SourceAttrs_2 as SourceAttrs,
    SourceAttrsOptions,
    WithSrcSetBuilder,
    propsToKebabCase,
    toInlineStyle,
  };
}

// Warning: (ae-forgotten-export) The symbol "HTMLImageAttributes" needs to be exported by the entry point api-extractor.d.ts
//
// @public
type ImgAttrs = Prettify<
  {
    src: string;
  } & {
    [Key in keyof HTMLImageAttributes]?: NonNullable<HTMLImageAttributes[Key]>;
  }
>;

// Warning: (ae-forgotten-export) The symbol "KebabCasedProperties" needs to be exported by the entry point api-extractor.d.ts
//
// @public (undocumented)
type ImgAttrs_2 = KebabCasedProperties<ImgAttrs>;

// Warning: (ae-forgotten-export) The symbol "Prettify" needs to be exported by the entry point api-extractor.d.ts
//
// @public
type ImgAttrsOptions = Prettify<WithSrcSetBuilder & Omit<ImgAttrs, 'height' | 'src' | 'width'>>;

declare namespace internal {
  export {
    hasIntrinsicDimensions as _hasIntrinsicDimensions,
    isKeyOf as _isKeyOf,
    invariant as _invariant,
    isEmpty as _isEmpty,
    isNullish as _isNullish,
    isNumber as _isNumber,
    isString as _isString,
  };
}

// @internal
function invariant(condition: unknown, message: string | (() => string)): asserts condition;

// @internal
function isEmpty(value: unknown): value is null | undefined | '';

// @internal
function isKeyOf<K extends keyof T, T extends object>(key: unknown, map: T): key is K;

// @internal
function isNullish(value: unknown): value is null | undefined;

// @internal
function isNumber(value: unknown): value is number;

// @internal
function isString(value: unknown): value is string;

declare namespace jsx {
  export {
    buildSrcSet,
    defineSrcSetBuilder,
    getImgAttrs,
    getSourceAttrs,
    withImgAttrs,
    withSourceAttrs,
    normalizeDirectives,
    BuildSrcSetOptions,
    ImgAttrs,
    ImgAttrsOptions,
    SourceAttrs,
    SourceAttrsOptions,
    WithSrcSetBuilder,
    getAttrs,
    getMimeType,
    mustBeInRange,
    mustBeOneOf,
    MIME_TYPES,
  };
}

// @public
export const mergeConfig: (options: YootConfig) => void;

// @internal
const MIME_TYPES: {
  readonly jpeg: 'image/jpeg';
  readonly jpg: 'image/jpeg';
  readonly png: 'image/png';
  readonly webp: 'image/webp';
};

// @internal
function mustBeInRange(start: number, end: number): (key: string, value: unknown) => void;

// @internal
function mustBeOneOf<T extends string>(allowed: Set<T>): (key: string, value: unknown) => void;

// @internal
function normalizeDirectives(input: YootState): Directives;

// @public
export interface OutputMethods {
  toJSON: () => YootState;
  toString: () => string;
  readonly url: string;
}

// @public
export const passThroughAdapter: Adapter;

// @public
export type PrimeStateInput = {
  src: string;
} & Omit<YootState, 'src'>;

// @internal
function propsToKebabCase<T extends Record<string, unknown>>(attributes: T): KebabCasedProperties<T>;

// @public
export const registerAdapters: (...adapters: Adapter[]) => void;

// Warning: (ae-forgotten-export) The symbol "HTMLSourceAttributes" needs to be exported by the entry point api-extractor.d.ts
//
// @public
type SourceAttrs = {
  [Key in keyof HTMLSourceAttributes]: NonNullable<HTMLSourceAttributes[Key]>;
};

// @public (undocumented)
type SourceAttrs_2 = KebabCasedProperties<SourceAttrs>;

// @public
type SourceAttrsOptions = Prettify<Omit<SourceAttrs, 'height' | 'src' | 'width'> & WithSrcSetBuilder>;

// @internal
function toInlineStyle(props: unknown): string;

// @public
export type Vertical = 'top' | 'bottom';

// @public
function withImgAttrs(options: ImgAttrsOptions): (
  yoot: Yoot,
  overrideOptions?: ImgAttrsOptions,
) => {
  [x: `data-${string}`]: NonNullable<Maybe<Primitive_2>> | undefined;
  src: string;
  width?: number | undefined;
  height?: number | undefined;
  alt?: string | undefined;
  crossOrigin?: NonNullable<'' | 'anonymous' | 'use-credentials' | null | undefined> | undefined;
  decoding?: NonNullable<Maybe<'auto' | 'async' | 'sync'>> | undefined;
  fetchPriority?: NonNullable<Maybe<'auto' | 'high' | 'low'>> | undefined;
  loading?: NonNullable<Maybe<'eager' | 'lazy'>> | undefined;
  referrerPolicy?:
    | NonNullable<
        | ''
        | 'no-referrer'
        | 'no-referrer-when-downgrade'
        | 'origin'
        | 'origin-when-cross-origin'
        | 'same-origin'
        | 'strict-origin'
        | 'strict-origin-when-cross-origin'
        | 'unsafe-url'
        | null
        | undefined
      >
    | undefined;
  sizes?: string | undefined;
  srcset?: string | undefined;
  title?: string | undefined;
  style?: Record<string, string | number> | undefined;
  'aria-label'?: string | undefined;
  'aria-labelledby'?: string | undefined;
  'aria-describedby'?: string | undefined;
  'aria-hidden'?: NonNullable<Maybe<boolean | 'true' | 'false'>> | undefined;
};

// @public
function withImgAttrs_2(options: ImgAttrsOptions): (yoot: Yoot, overrideOptions?: ImgAttrsOptions) => ImgAttrs_2;

// @public
function withSourceAttrs(
  options: SourceAttrsOptions,
): (yoot: Yoot, overrideOptions?: SourceAttrsOptions) => SourceAttrs;

// @public
function withSourceAttrs_2(
  options: SourceAttrsOptions,
): (yoot: Yoot, overrideOptions?: SourceAttrsOptions) => SourceAttrs_2;

// @public
type WithSrcSetBuilder = {
  srcSetBuilder?: (yoot: Yoot) => string;
};

// @public
export interface Yoot extends YootFactory, DirectiveMethods, OutputMethods {
  // @internal
  readonly [YOOT_BRAND]: true;
  alt(alt: string): Yoot;
  map(fn: (state: YootState) => YootState): Yoot;
  src(src: string | undefined): Yoot;
}

// @public
export const yoot: (init?: YootInput) => Yoot;

// @public
export type YootConfig = {
  onMissingAdapter?: (url: URL) => Adapter | never;
};

// @public
export interface YootFactory {
  // (undocumented)
  (input?: YootInput): Yoot;
}

// Warning: (ae-forgotten-export) The symbol "SomeYootState" needs to be exported by the entry point api-extractor.d.ts
//
// @public
export type YootInput = string | Yoot | SomeYootState;

// @public
export type YootState = {
  src?: string;
  alt?: string;
  width?: number;
  height?: number;
  directives: Directives;
};

// Warnings were encountered during analysis:
//
// dist/core/helpers.d.ts:169:5 - (ae-forgotten-export) The symbol "Maybe" needs to be exported by the entry point api-extractor.d.ts
```
